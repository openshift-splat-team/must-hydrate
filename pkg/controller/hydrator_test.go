package controller

import (
	"testing"
)

func TestYamlPaths(t *testing.T) {
	// rootDir := "/run/media/rvanderp/fedora_localhost-live/home/rvanderp/code/must-hydrate/data"
	// yamls := getYamlsInPath("/run/media/rvanderp/fedora_localhost-live/home/rvanderp/code/must-hydrate/data")

	// t.Logf("found %d yamls\n", len(yamls))

	// crdPath := getCrdPaths(rootDir)

	// fmt.Printf("crd path: %v\n", crdPath)
}
